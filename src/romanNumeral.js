
const numberDict = {I: 1, V: 5, X: 10, L: 50, C: 100, M:1000};
const arabicNumber = 0;

export const romanNumeralConverter = (romanNumber) => {
  // termination condition
  if (romanNumber.length === 0) {
    return 0;
  }
  // base condition
  // if () {
  // } else {
      // if (romanNumber[0] < romanNumber[1]) {

      // }

    // console.log(number);


  // }



}